# Flask Configuration
FLASK_DEBUG=False
DATABASE_URL=sqlite:///copilot_demo.db

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Security Note:
# - Never set FLASK_DEBUG=True in production
# - Update ALLOWED_ORIGINS to match your production domains
# - Use a production-grade database (PostgreSQL, MySQL) instead of SQLite
